# Clerk can run in keyless mode for local dev.
# For explicit credentials, set these values:
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Required when using webhook verification from Clerk Dashboard.
CLERK_WEBHOOK_SIGNING_SECRET=
